# Claude Code Tools vs Python Scripts - Фундаментальная разница

**Дата создания:** 2025-11-03
**Источник:** Беседа с Claude Code

---

## Оглавление
1. [Фундаментальная разница](#фундаментальная-разница)
2. [Claude Code Tools](#claude-code-tools-встроенные)
3. [Python Scripts](#python-scripts-создаваемые-агентом)
4. [Техническое сравнение](#техническое-сравнение)
5. [Может ли агент создавать новые Tools](#может-ли-агент-создавать-новые-tools)
6. [Переиспользование скриптов](#переиспользование-скриптов)

---

## Фундаментальная разница

### Аналогия со смартфоном:

```
Claude Code Tools = встроенные функции телефона
├─ Камера (Read tool)
├─ Калькулятор (Bash tool)
└─ Фонарик (Write tool)

Python Scripts = приложения которые вы устанавливаете
├─ Instagram (apollo_icp_validator.py)
├─ WhatsApp (email_validator.py)
└─ Uber (linkedin_scraper.py)
```

**Встроенные функции:**
- Быстрые, всегда доступны
- Часть операционной системы
- Нельзя удалить или изменить
- Ограниченный, но надёжный функционал

**Приложения:**
- Медленнее (нужно запустить)
- Можно установить/удалить
- Можно модифицировать
- Безграничный функционал

---

## Claude Code Tools (встроенные)

### Что это такое:

```typescript
// Написаны на TypeScript, встроены в Claude Code

// Пример: Read tool
async function readFile(file_path: string) {
  // Прямой доступ к файловой системе
  const content = await fs.readFile(file_path, 'utf-8');
  return content;
}
```

### Как Claude их вызывает:

```xml
<!-- Claude делает вызов напрямую -->
<invoke name="Read">
  <parameter name="file_path">file.txt</parameter>
</invoke>

<!-- Результат возвращается мгновенно -->
```

### Характеристики:

```
✅ Часть Claude Code (встроены в бинарник)
✅ Написаны на TypeScript/JavaScript
✅ Работают ВНУТРИ процесса Claude Code
✅ Прямой доступ к внутренним API Claude
✅ Быстрые (нет overhead запуска)
✅ Claude видит их автоматически
✅ Могут возвращать структурированные данные
✅ Могут показывать progress bars, UI элементы
✅ Безопасны (проверены Anthropic)
```

### Где они находятся:

```bash
# Встроены в Claude Code исходники
claude-code/
  ├─ src/tools/
  │   ├─ read.ts       # Read tool
  │   ├─ write.ts      # Write tool
  │   ├─ bash.ts       # Bash tool
  │   ├─ grep.ts       # Grep tool
  │   └─ webfetch.ts   # WebFetch tool
  └─ dist/
      └─ claude.exe    # Скомпилированный бинарник

# Вы НЕ МОЖЕТЕ их изменить (часть приложения)
```

### Список встроенных Tools:

```
Read       - чтение файлов
Write      - создание файлов
Edit       - редактирование файлов
Bash       - выполнение команд
Glob       - поиск файлов по паттерну
Grep       - поиск в содержимом
WebFetch   - HTTP запросы
WebSearch  - поиск в интернете
Task       - запуск агентов
TodoWrite  - управление задачами
```

---

## Python Scripts (создаваемые агентом)

### Что это такое:

```python
# modules/apollo/apollo_icp_validator.py
# Обычный Python файл в вашей папке

import pandas as pd
import os

def normalize_company_name(name: str) -> str:
    """Нормализация названия компании"""
    return name.strip().replace(', LLC', '')

def main():
    df = pd.read_csv("input.csv")
    df['normalized_name'] = df['company_name'].apply(normalize_company_name)
    df.to_csv("output.csv", index=False)
    print("Done!")

if __name__ == "__main__":
    main()
```

### Как агент его использует:

```python
# Агент НЕ МОЖЕТ вызвать Python напрямую!
# Он делает это в 3 шага:

# Шаг 1: Write tool → создает файл
Write(file_path="apollo_icp_validator.py", content="...")

# Шаг 2: Bash tool → запускает через subprocess
Bash(command="python apollo_icp_validator.py")

# Шаг 3: Read tool → читает результат
Read(file_path="output.csv")
```

### Характеристики:

```
❌ НЕ часть Claude Code (внешние файлы в проекте)
✅ Написаны на Python/JS/любом языке
❌ Работают СНАРУЖИ процесса Claude (subprocess)
❌ НЕТ доступа к внутренним API Claude
❌ Медленнее (нужно запускать через Bash → Python)
❌ Claude не видит их напрямую (нужен Bash tool)
✅ Могут делать что угодно (API calls, DB queries)
✅ Легко распространять (просто файлы)
✅ Можно модифицировать как угодно
✅ Используют любые Python библиотеки
```

### Где они находятся:

```bash
# Обычные файлы в папке вашего проекта
modules/apollo/
  ├─ apollo_icp_validator.py      # Основной скрипт
  ├─ analyze_icp_results.py       # Анализ
  └─ tools/
      ├─ email_validator.py       # Вспомогательные функции
      └─ company_normalizer.py

# Вы ПОЛНОСТЬЮ контролируете эти файлы
```

---

## Техническое сравнение

| Аспект | Claude Code Tools | Python Scripts |
|--------|-------------------|----------------|
| **Язык** | TypeScript/JavaScript | Python/JS/Bash/любой |
| **Где работают** | Внутри Claude Code | Subprocess (отдельный процесс) |
| **Как вызываются** | `<invoke name="Read">` | `Bash: python script.py` |
| **Скорость** | ⚡ Мгновенно (< 1ms) | 🐌 Запуск Python (~100-500ms) |
| **Доступ к Claude API** | ✅ Полный доступ | ❌ Нет |
| **API calls (Apollo, etc)** | ⚠️ Ограничены | ✅ Любые |
| **Распространение** | 🔒 Только с Claude Code | ✅ Просто файлы (.py) |
| **Модификация** | ❌ Нельзя (closed source) | ✅ Легко редактировать |
| **Зависимости** | 📦 Встроены | 📦 pip install нужен |
| **Создание агентом** | ❌ Невозможно | ✅ Write + Bash |
| **Безопасность** | ✅ Проверено Anthropic | ⚠️ Ваша ответственность |

---

## Может ли агент создавать новые Tools?

### Короткий ответ: **НЕТ!**

### Длинный ответ:

```python
# Claude Code Tools = встроенные функции Claude Code
# Определены в исходниках приложения

Read tool   # src/tools/read.ts - часть Claude Code
Write tool  # src/tools/write.ts - часть Claude Code
Bash tool   # src/tools/bash.ts - часть Claude Code

# Агент НЕ может добавить:
CustomTool      # ❌ Невозможно
NewApolloTool   # ❌ Невозможно
EmailValidator  # ❌ Невозможно (как tool)
```

### НО! Агент может создавать "псевдо-инструменты":

```python
# modules/apollo/tools/email_validator.py
# Это НЕ Claude Code Tool, но работает как инструмент

import requests
import os

def validate_email(email: str) -> bool:
    """Проверяет email через Hunter.io API"""
    api_key = os.getenv("HUNTER_API_KEY")
    response = requests.get(
        f"https://api.hunter.io/v2/email-verifier?email={email}",
        headers={"Authorization": f"Bearer {api_key}"}
    )
    return response.json()['data']['result'] == 'deliverable'

def enrich_linkedin(company_name: str) -> str:
    """Ищет LinkedIn профиль компании"""
    # Логика поиска через API
    return linkedin_url
```

### Как агент это использует:

```python
# Запуск 1 (агент создаёт "инструмент"):
Step 1: Write tool → создаёт tools/email_validator.py
Step 2: Bash tool → python -c "from tools.email_validator import validate_email; print(validate_email('test@example.com'))"
Step 3: Получает результат: True/False

# Запуск 2 (агент переиспользует существующий):
Step 1: Bash tool → python -c "from tools.email_validator import validate_email; ..."

# Это НЕ настоящий Claude Code Tool!
# Это просто Python функция, которую агент вызывает через Bash
```

### Единственный способ создать настоящий Tool - MCP Server:

```typescript
// apollo-mcp-server/src/index.ts
// Это НАСТОЯЩИЙ новый tool!

import { Server } from "@modelcontextprotocol/sdk/server/index.js";

const server = new Server({
  name: "apollo-api",
  version: "1.0.0",
});

// Регистрируем НОВЫЙ Claude Code Tool
server.setRequestHandler("tools/list", async () => ({
  tools: [{
    name: "apollo_search",  // Новый tool!
    description: "Search companies in Apollo",
    inputSchema: { /* ... */ }
  }]
}));

// Теперь Claude может вызывать:
// <invoke name="apollo_search">
```

**См. подробнее:** [MCP Servers Guide](./mcp-servers-guide.md)

---

## Переиспользование скриптов

### Агент с ограниченными tools:

```markdown
# .claude/agents/readonly-analyzer.md
---
name: readonly-analyzer
tools: ["Read", "Grep"]  # ТОЛЬКО чтение!
---

Анализируй файлы, но НИКОГДА не изменяй их.
```

**Такой агент НЕ МОЖЕТ:**
```
❌ Write tool - создавать файлы
❌ Edit tool - редактировать файлы
❌ Bash tool - запускать скрипты
❌ Создать новые Python скрипты
❌ Расширить свои возможности
❌ Изменить свой .md файл чтобы добавить tools
```

**Он МОЖЕТ только:**
```
✅ Read - читать существующие файлы
✅ Grep - искать в файлах
✅ Анализировать и возвращать текстовый отчёт
```

**Вопрос:** Может ли он добавить себе инструменты?
**Ответ:** **НЕТ!** Агент ограничен tools из своего .md файла навсегда.

---

### Агент со всеми tools - переиспользование:

```markdown
# .claude/agents/smart-processor.md
---
name: smart-processor
tools: ["*"]  # ВСЕ инструменты!
---

Перед созданием скрипта:
1. Проверь существует ли modules/apollo/apollo_icp_validator.py
2. Если да - используй его через Bash tool
3. Если нет или нужны изменения - создай новый через Write tool
```

**Как это работает:**

```python
# Запуск 1 (сегодня, скрипта нет):
Агент: Glob → ищет apollo_icp_validator.py
Результат: не найден

Агент: Write → создаёт apollo_icp_validator.py
Агент: Bash → python apollo_icp_validator.py
Файл ОСТАЁТСЯ в папке

# Запуск 2 (завтра, скрипт уже есть):
Агент: Glob → ищет apollo_icp_validator.py
Результат: найден!

Агент: Read → проверяет содержимое
Агент: Bash → python apollo_icp_validator.py (переиспользует!)
✅ Быстрее, стабильнее
```

---

## Практический пример

### Сценарий: Обработка Apollo CSV

#### БЕЗ агента (я делаю сам):

```
Вы: "Обработай CSV - удали дубликаты"

Я (Claude):
├─ Read: modules/apollo/results/call_centers.csv
├─ Write: modules/apollo/scripts/remove_duplicates.py
│   import pandas as pd
│   df = pd.read_csv('call_centers.csv')
│   df_clean = df.drop_duplicates(subset=['email'])
│   df_clean.to_csv('clean.csv', index=False)
├─ Bash: python modules/apollo/scripts/remove_duplicates.py
└─ Результат: clean.csv создан

Файлы которые ОСТАЛИСЬ:
✅ modules/apollo/scripts/remove_duplicates.py
✅ modules/apollo/results/clean.csv
```

#### С агентом apollo-icp-analyzer:

```
Вы: "Обработай CSV"

Я: Task tool → apollo-icp-analyzer

Агент (в своём контексте):
├─ Read: modules/apollo/results/call_centers.csv
├─ Write: modules/apollo/scripts/remove_duplicates.py
│   (тот же код что выше)
├─ Bash: python modules/apollo/scripts/remove_duplicates.py
└─ Возвращаю отчёт основному Claude

Я: "✅ CSV обработан, 50 дубликатов удалено"

Файлы которые ОСТАЛИСЬ:
✅ modules/apollo/scripts/remove_duplicates.py (ТАКОЙ ЖЕ!)
✅ modules/apollo/results/clean.csv (ТАКОЙ ЖЕ!)
```

### Ключевой вывод:

**ФАЙЛЫ И СКРИПТЫ - АБСОЛЮТНО ОДИНАКОВЫЕ!**

Разница только в **процессе выполнения**:
- БЕЗ агента: я работаю в контексте вашего диалога
- С агентом: агент работает изолированно, по своим инструкциям

---

## Стратегия накопления библиотеки

### Создайте переиспользуемую библиотеку:

```bash
modules/apollo/tools/
├─ email_validator.py       # Проверка email через Hunter.io
├─ linkedin_scraper.py      # Поиск LinkedIn профилей
├─ icp_scorer.py            # ICP scoring логика
├─ company_normalizer.py    # Нормализация названий
└─ phone_formatter.py       # Форматирование телефонов
```

### Агент использует библиотеку:

```markdown
# .claude/agents/apollo-processor.md
---
name: apollo-processor
tools: ["*"]
---

Ты обрабатываешь Apollo CSV файлы.

ВАЖНО: Используй существующие функции из modules/apollo/tools/

Процесс:
1. Read CSV файл
2. Для нормализации компаний:
   python -c "from modules.apollo.tools.company_normalizer import normalize; ..."
3. Для валидации email:
   python -c "from modules.apollo.tools.email_validator import validate; ..."
4. Сохрани результат
```

### Преимущества:

```
✅ Переиспользование кода (DRY principle)
✅ Быстрее (не нужно создавать скрипты каждый раз)
✅ Стабильнее (проверенные функции)
✅ Легче поддерживать (один раз исправил - везде работает)
✅ Библиотека растёт со временем
```

---

## Резюме

### Claude Code Tools:
- Встроенные в приложение
- Быстрые и надёжные
- Нельзя изменить
- Ограниченный функционал
- Агент не может создавать новые

### Python Scripts:
- Создаются агентом/вами
- Гибкие и мощные
- Легко модифицировать
- Безграничные возможности
- Медленнее (subprocess overhead)

### Главное правило:

```
Claude Code Tools = фундамент (Read, Write, Bash)
Python Scripts = ваши инструменты (apollo_validator.py)

Агент использует Tools чтобы создать и запустить Scripts
```

---

## Связанные материалы

- [Агенты - полное руководство](./agents-explained.md)
- [Стратегии деплоймента](./deployment-strategies.md)
- [MCP Servers - создание настоящих Tools](./mcp-servers-guide.md)

---

**Обновлено:** 2025-11-03
**Статус:** Актуально для Claude Code latest
