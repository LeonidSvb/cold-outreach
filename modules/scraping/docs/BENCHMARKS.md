# Website Scraper - Benchmarks и Time Estimates

> Реальные бенчмарки для планирования scraping задач

---

## 📊 Основные метрики (25 workers)

| Режим | Операции | Время на 1 сайт | 100 сайтов | 1,000 сайтов | 10,000 сайтов |
|-------|----------|-----------------|------------|--------------|---------------|
| **quick** | Static/Dynamic detection | ~0.05 сек | ~5 сек | ~50 сек | ~8 мин |
| **standard** | Scraping + Emails | ~0.5 сек | ~50 сек | ~8 мин | ~80 мин |
| **full** | Scraping + AI | ~3.0 сек | ~5 мин | ~50 мин | ~8 часов |

---

## 🔧 Влияние параметров

### Workers (параллельные потоки)

| Workers | 100 сайтов (standard) | 1,000 сайтов (standard) | 10,000 сайтов (standard) |
|---------|----------------------|-------------------------|--------------------------|
| 10 | ~2 мин | ~20 мин | ~3.3 часа |
| 25 | ~50 сек | ~8 мин | ~80 мин |
| 50 | ~25 сек | ~4 мин | ~40 мин |
| 100 | ~15 сек | ~2.5 мин | ~25 мин |

**Рекомендация:**
- **10-25 workers** - стабильно, низкий риск rate limiting
- **50 workers** - быстро, но возможны timeout'ы на медленных сайтах
- **100+ workers** - очень быстро, но высокий риск блокировок

---

## 🎯 Режимы работы

### Mode: `quick`
**Что делает:**
- HTTP запрос к homepage
- Проверка static/dynamic
- Сохраняет только status

**Когда использовать:**
- Нужно быстро проверить 10,000+ сайтов
- Планируете отфильтровать dynamic сайты для Firecrawl
- Нужна только валидация URL'ов

**Пример:**
```bash
python website_scraper.py --input 10000_urls.csv --output checked.csv --mode quick --workers 100
# Время: ~8 минут
# Результат: CSV с колонками [url, status, site_type]
```

---

### Mode: `standard`
**Что делает:**
- HTTP запрос к homepage + важные страницы (/contact, /about)
- Извлечение emails
- Извлечение телефонов
- Очистка HTML → текст

**Когда использовать:**
- Нужны контакты для cold outreach
- Не требуется AI анализ (экономия $$$)
- Большинство use cases

**Пример:**
```bash
python website_scraper.py --input 1000_hvac.csv --output hvac_contacts.csv --mode standard --workers 25
# Время: ~8 минут
# Результат: CSV с [url, status, emails, phones, content]
```

---

### Mode: `full`
**Что делает:**
- Всё из `standard` режима
- AI анализ контента (personalization, ICP validation, etc)
- Генерация structured data

**Когда использовать:**
- Нужна персонализация для outreach
- ICP validation
- Категоризация компаний

**Пример:**
```bash
python website_scraper.py --input 100_prospects.csv --output enriched.csv --mode full --workers 10
# Время: ~5 минут
# Стоимость AI: ~$0.10 (OpenAI gpt-4o-mini)
# Результат: CSV с [url, emails, ai_summary, personalization_hook, owner_name, etc]
```

---

## 💰 Стоимость AI анализа

**OpenAI gpt-4o-mini** (рекомендуется):
- Input: $0.150 / 1M tokens
- Output: $0.600 / 1M tokens

**Расчёт:**
- 1 сайт ≈ 5,000 tokens input + 500 tokens output
- 1 сайт ≈ $0.001 (одна десятая цента)
- 1,000 сайтов ≈ **$1.00**
- 10,000 сайтов ≈ **$10.00**

**OpenAI gpt-4o** (более точный):
- Input: $2.50 / 1M tokens
- Output: $10.00 / 1M tokens
- 1,000 сайтов ≈ **$15.00**

---

## 📈 Оптимизация производительности

### Распространённые проблемы

| Проблема | Симптом | Решение |
|----------|---------|---------|
| **Много timeout'ов** | >10% status=timeout | Увеличить `--timeout 30` |
| **Много dynamic sites** | >20% status=dynamic | Использовать Firecrawl для fallback |
| **Медленная скорость** | <5 items/sec | Увеличить `--workers 50` |
| **Много ошибок 429** | status=http_error_429 | Уменьшить workers, добавить delay |

### Рекомендуемые конфигурации

**Для быстрого тестирования (100 сайтов):**
```bash
--mode quick --workers 50 --timeout 10
# Время: ~10 секунд
```

**Для production (1,000+ сайтов):**
```bash
--mode standard --workers 25 --timeout 15
# Баланс скорости и стабильности
```

**Для качественного enrichment (AI):**
```bash
--mode full --workers 10 --timeout 20 --max-text-length 15000
# Меньше workers = меньше rate limiting от OpenAI
```

---

## 🧪 Тестовые результаты

### Test 1: HVAC компании в Техасе
```
Input:     1,000 HVAC сайтов
Mode:      standard
Workers:   25
Duration:  8.2 минуты
Results:
  - Success:       720 (72%)
  - Dynamic:       180 (18%)
  - Timeout:        70 (7%)
  - Other errors:   30 (3%)
  - Emails found:  580 (58%)
```

### Test 2: Random B2B websites
```
Input:     500 B2B сайтов
Mode:      full (с AI)
Workers:   10
Duration:  25 минут
AI Cost:   $0.52
Results:
  - Success:       420 (84%)
  - Dynamic:        50 (10%)
  - Failures:       30 (6%)
  - AI analysis:   420/420 (100%)
  - Owner names:   280 (67% of analyzed)
```

---

## 📝 Оценка времени перед запуском

Используйте флаг `--estimate-only` для расчёта времени:

```bash
python website_scraper.py --input huge_list.csv --output results.csv --mode standard --estimate-only

# Output:
# ================================================================================
# UNIVERSAL WEBSITE SCRAPER
# ================================================================================
# Input file:        huge_list.csv
# Total URLs:        10,000
# Mode:              standard
# Workers:           25
#
# Estimated time:    80.0 min (4800 sec)
# ================================================================================
# Estimation only mode - exiting without scraping.
```

---

## 🎯 Выбор правильной конфигурации

### Вопросы для принятия решения:

1. **Сколько у меня сайтов?**
   - <100 → используйте `--workers 50` для скорости
   - 100-1000 → используйте `--workers 25` (оптимально)
   - >1000 → используйте `--workers 25`, но разбейте на батчи

2. **Нужен ли мне AI анализ?**
   - Да → `--mode full` (дороже, медленнее, но качественнее)
   - Нет → `--mode standard` (быстро, бесплатно)

3. **Какой у меня budget времени?**
   - <5 минут → `--mode quick`
   - <30 минут → `--mode standard` (<2000 сайтов)
   - Несколько часов → `--mode full` (любое количество)

4. **Какой success rate мне нужен?**
   - >90% → Используйте Firecrawl fallback для dynamic sites
   - 70-80% → Standard HTTP scraping достаточно

---

## 🔄 Workflow примеры

### Workflow 1: Быстрая фильтрация + глубокий анализ

```bash
# Шаг 1: Быстро проверить 10,000 сайтов (8 мин)
python website_scraper.py --input all_sites.csv --output checked.csv --mode quick --workers 100

# Шаг 2: Отфильтровать только static сайты
# (в Python/pandas)
df = pd.read_csv('checked.csv')
static_sites = df[df['site_type'] == 'static']
static_sites.to_csv('static_only.csv')

# Шаг 3: Глубокий анализ static сайтов (20 мин)
python website_scraper.py --input static_only.csv --output enriched.csv --mode full --workers 10
```

### Workflow 2: Обработка с fallback

```bash
# Шаг 1: Standard scraping (8 мин)
python website_scraper.py --input leads.csv --output results.csv --mode standard --workers 25

# Шаг 2: Вытащить dynamic sites для Firecrawl
df = pd.read_csv('results.csv')
dynamic = df[df['status'] == 'dynamic']
dynamic.to_csv('dynamic_sites.csv')

# Шаг 3: Обработать dynamic через Firecrawl (отдельный скрипт)
# python firecrawl_scraper.py --input dynamic_sites.csv --output firecrawl_results.csv

# Шаг 4: Объединить результаты
```

---

## ⚡ Quick Reference

```bash
# Самый быстрый способ (только проверка)
--mode quick --workers 100

# Оптимальный баланс (scraping + emails)
--mode standard --workers 25

# Максимальное качество (с AI)
--mode full --workers 10

# Кастомная конфигурация
--check-static --extract-emails --scrape-mode smart --workers 50
```
